#!/usr/bin/env groovy

pipeline {

    agent {
        node {
        label 'Slave'
    }
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
        }
       
        }
    }
}
